---
interface SectionContainerProps {
  id: string;
  className?: string;
}

const { id, className = '' } = Astro.props as SectionContainerProps;
---

<section
  id={id}
  data-section={id}
  class={`section ${className} scroll-m-20 w-full mx-auto h-screen py-20`}
>
  <slot />
</section>
